{
  "name": "api-to-mcp-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Convert OpenAPI specs to MCP Servers",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev": "pnpm -r dev",
    "start": "node packages/cli/dist/index.js"
  },
  "engines": { "node": ">=20.0.0" },
  "packageManager": "pnpm@9.0.0",
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/node": "^20.14.0"
  }
}